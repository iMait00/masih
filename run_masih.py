"""
نقطة الدخول التي يبني عليها PyInstaller.

لا يجوز توجيه PyInstaller إلى src/masih/__main__.py مباشرة: فهو يشغّله
بوصفه سكربتاً مستقلاً اسمه __main__، فتنكسر الاستيرادات النسبية داخله
(from . import ...). وفي بناءٍ بلا نافذة أوامر لا يظهر الخطأ في مكان،
بل يُفتح صندوق رسالة مخفيّ يبقى البرنامج معلّقاً عنده.

فالحلّ أن نستورد الحزمة استيراداً عادياً ثم نستدعي main.
"""

import sys

from masih.__main__ import main

if __name__ == "__main__":
    sys.exit(main())
